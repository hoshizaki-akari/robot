# 实时方向纠偏 V1.0 开发记录

## 当前结论

V1.0 已完成“算法模块 + ROS2 消息链 + shadow 模式”开发。默认配置为
`direction_correction_mode: shadow`，因此算法会计算并记录侧向纠偏量，但不会
向真实 FR5 发送侧向速度。只有完成实体方向符号检查后，才允许把配置改成
`active`。

## 处理链

1. manager 先用松弛校零后的 `base_link` 力矢量作为输入。
2. 锁定方向 `d0` 保持不变，不随每个瞬时采样漂移。
3. 张力大小使用 `T = ||F||`；方向使用单位向量 `u = F / ||F||`。
4. 用 7 点球面 medoid（单位向量中值）抑制单个跳点。连续方向变化会在
     7 个采样窗口后进入快滤波器，因此不会被单点中值永久吞掉。
5. 快滤波器默认 4 Hz，慢滤波器默认 0.6 Hz。快、慢方向之间的夹角和快方向
   相对 `d0` 的夹角共同作为变化证据。
6. 校准窗口估计角度的 MAD。进入纠偏阈值为
   `max(3 deg, median + 4 * 1.4826 * MAD)`，退出阈值为进入阈值的一半且不低于
   1.5 deg。这样阈值会随传感器静态噪声变化，但不会低到追逐量化噪声。
7. 变化证据连续默认 0.15 s 才进入 `CORRECTING`；力过小或方向反向则进入
   `SENSOR_HOLD`，侧向指令为零。
8. 纠偏在锁定方向的切平面 `b1/b2` 中进行。切向误差为
   `e = [b1·(F - (F·d0)d0), b2·(F - (F·d0)d0)]`。
9. 侧向响应矩阵使用阻尼最小二乘：
   `q = J^T (J J^T + lambda^2 I)^-1 e`，
   `v_tangent = -K q`。负号表示沿“消除侧向力”的方向移动；控制器最后再
   把切向速度变回 `base_link`。
10. V1.0 默认侧向速度上限 0.5 mm/s，纠偏累计位移上限 3 mm，且只在
    `CORRECTING/SETTLING` 状态输出；单点跳变、反向力、失去新鲜力数据都不动。

## 当前 FR5 响应参数

2026-09-02 固定目标 base X/Y 小范围测试得到（原始单位 N/mm，配置换成 N/m）：

```text
J_base_xy = [[ 6.47, 5.84 ],
             [10.79, 0.76 ]] N/m
```

每次方向锁定后，程序将这个 base X/Y 矩阵转换到当前锁定方向对应的
`b1/b2` 坐标；不会把 base 坐标矩阵直接当作切向矩阵使用。

## 已完成的自动验证

- 切平面基向量正交归一、重复计算确定性：通过。
- 静态噪声 MAD 阈值和最小阈值：通过。
- 单个 30° 跳点被判为 outlier，持续 15° 变化最终进入 `CORRECTING`：通过。
- 低力和反向力进入 `SENSOR_HOLD`：通过。
- DLS 反馈符号、0.5 mm/s 速度限幅、3 mm 累计位移限幅：通过。
- shadow 模式能够计算期望速度但实际速度保持 0：通过。
- 原有 20 个 traction math/controller/safety/state/pipeline 测试：通过。
- 真实运行栈力话题约 25 Hz，网页 READY、无故障：通过。

## 实体影子测试结果

本次按“松弛校准 → tool Y− 搜索 → 回零”执行。搜索有明确边界 30 mm，结果是
末端移动约 30 mm，力仍约 0.4 N，没有张紧；程序没有把这个结果误判为方向，
也没有继续盲目换方向，之后已回到保存的松弛零点。该结果说明当前工位的
tool Y− 搜索方向/带子布置需要人工确认，不能直接作为 V1.0 纠偏算法结论。

## 当前检查点

状态为：`MANUAL_SETUP`，机械臂已回到松弛零点，配置仍为 `shadow`。

进入 active 前，只需要人工确认：

1. 用法奥官方示教器从该松弛零点把带子张紧。
2. 保持法兰静止后点击“方向确定”。
3. 在 shadow 模式下进行一次 5 N 牵引，观察记录中的
   `direction_track_state`、`direction_error_rad`、`direction_vx/vy/vz`；确认
   方向稳定时没有持续大幅纠偏请求。
4. 再由操作者确认第一次实体纠偏的正负方向。未经这个确认，不切换 active。
