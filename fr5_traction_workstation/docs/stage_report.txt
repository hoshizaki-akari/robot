===== FR5 TRACTION WORKSTATION =====

date = 2026-09-01
robot = FR5 @ 192.168.58.2
sensor = KWR75D
force_frame = base_link

software:
- direct single-owner Fairino SDK driver, 25 Hz feedback and motion path
- baseline-corrected wrench: /traction/corrected_wrench
- tool Y- bounded tension search: max 30 mm, stable target about 1.5 N
- target and startup range 1..20 N; 20 N still needs a real test
- initial calibration now stores the slack baseline and the only return-zero pose
- the direct driver separately stores the pre-traction pose when Cartesian traction starts;
  normal end unloads force and returns there automatically
- startup scripts fixed for ROS2 environment setup and launch argument syntax
- initial calibration is available in stopped/fault-recovery states and replaces a stale
  baseline only when the operator confirms the band is slack
- live Tool Y- test showed the deployed base command sign was reversed; configured
  base_servo_sign = 1.0 before validation
- feedback/motion RPC rate and stale-data windows were adjusted for the real FR5;
  controller timing now uses a steady clock and pauses safely on long scheduling gaps
- return-zero reads the current return distance parameter; the separate set-zero UI action
  has been removed

offline_tests:
- C++: 5/5 passed
- Python/UI: 4/4 passed
- py_compile: passed; ruff is not installed in the current ROS environment

real_validation:
- tool Y- success: 1.53 N at 9.40 mm; 1.58 N at 12.12 mm;
  1.91 N at 29.35 mm
- 5 N: complete real run and normal release passed;
  session_1788235241553, NORMAL_RELEASE_COMPLETED
- 10 N: not passed; max approximately 7.92 N at about 80 mm axial travel;
  session_1788235931318, AXIAL_TRAVEL_LIMIT safety stop
- session_1788248515416: WRENCH_TIMEOUT followed a measured ~0.5 s feedback gap
- session_1788248573130: 15 N control oscillated at +/-5 mm/s and overshot to
  26.30 N; OVERFORCE stopped motion

latest_fault_fix:
- axial force no longer produces OVERFORCE or HARD_OVERFORCE; force above target is
  sent through the same signed controller error so the arm can counter an external pull
- normal end now waits for force release and pre-traction position return before completing
- every new run sends the current UI target immediately before start
- deadband now brakes velocity and clears integral immediately
- force-error sign reversal drops stale velocity before correcting direction
- stale feedback now pauses motion at 0.25 s and faults only if it persists to 0.8 s
- direct-driver stale command timeout reduced to 0.25 s
- baseline-restart debug sessions are hidden from operator abnormal records
- recorded-force replay: old controller still increased force at 25.75 N; fixed
  controller stopped increasing before 15 N

ui_button_validation_previous:
- target buttons -5N, -10N, +5N, +10N: clicked with 1.2 s response checks;
  UI and ROS target values stayed synchronized and clamped to 1..20 N
- prepare: clicked at slack zero; READY -> MANUAL_SETUP
- direction lock: real Tool Y- bounded search followed by UI click;
  MANUAL_SETUP -> DIRECTION_LOCKED and vectors appeared on the page
- start: UI click entered TRACTION and started the 1 N record;
  session_1788236839724
- normal release: previous UI click reached COMPLETED and saved the record
- software emergency stop: previous UI click reached EMERGENCY_STOP; reset returned to READY
- set zero and low-speed return: both UI clicks succeeded; final pose returned to
  z ~= 0.486 m with force below 0.3 N
- records, CSV export, and modal close: all succeeded; browser error/warn log empty
- operator-role settings button remained disabled by permission as designed;
  logout was not clicked so the validated operator session and final READY state
  remained available

current_state = MANUAL_SETUP
current_motion = stopped near z ~= 0.536 m; target = 15 N; corrected force ~= 0.7 N
status = STATE_FLOW_UPDATED; BUILT_AND_OFFLINE_TESTED; WAITING_SLACK_CONFIRMATION_FOR_RESTART

checkpoint:
Before restarting the ROS stack, the operator must confirm that the traction band is
physically slack. Restarting at a tensioned pose would incorrectly capture tension as
the new zero. After confirmation, restart and perform staged 5 N then 10 N/15 N real tests,
including normal end and automatic return to the pre-traction pose.
