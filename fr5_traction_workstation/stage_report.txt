===== DIRECTION FOLLOWING FIELD-LOG FIX =====

records:
session_1788416733567 = normal end; 59.6 s SUSPECT / 188.8 s traction
session_1788417254772 = calibration fault while force magnitude was changing
session_1788417322003 = normal end; longest false SUSPECT segment 15.8 s

root_causes:
hysteresis band was incorrectly treated as a no-motion SUSPECT state
direction calibration rejected force-magnitude drift instead of angular drift
position return used a fixed 2 mm/s linear interpolation

fixes:
sub-entry angular wander now remains STABLE; confirmed large changes still follow
direction calibration operates on unit vectors and angular robust residuals
return uses a 20 mm/s, 100 mm/s^2 trapezoidal position trajectory
force control remains disabled throughout return

expected_return_time_from_records:
40.2 mm = approximately 2.2 s instead of 20.4 s
65.7 mm = approximately 3.5 s instead of 35.2 s

automatic_verification:
ROS2 build passed
86 tests, 0 errors, 0 failures
new magnitude-invariant calibration and hysteresis-dead-zone regressions passed
Python syntax/style checks passed

status:
OFFLINE_FIX_COMPLETE_WAITING_FOR_NEXT_REAL_RUN
